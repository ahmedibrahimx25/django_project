[project]
name = "django-project-worker"
version = "0.1.0"
description = "Django Process REST API backed by D1 via django-cf"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "django",
    "djangorestframework",
    "django-cf>=0.2.14",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]